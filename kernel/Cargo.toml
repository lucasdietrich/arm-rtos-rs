[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[features]
default = ["mps2-an385", "kernel-stats"]
mps2-an385 = ["cm3"]
cm3 = []
kernel-stats = []
kernel-debug = []

[dependencies]
num-traits = { version =  "0.2", default-features = false}
num-derive = { version =  "0.4", default-features = false}

volatile-register = "0.2.2"
bitfield = "0.16.0"
